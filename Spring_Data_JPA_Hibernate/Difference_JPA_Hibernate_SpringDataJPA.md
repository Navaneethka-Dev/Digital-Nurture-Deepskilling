# Differences

**JPA (Java Persistence API):** A specification/standard for ORM in Java. It defines the interfaces but provides no implementation.
**Hibernate:** A popular implementation (provider) of the JPA specification.
**Spring Data JPA:** A layer on top of JPA/Hibernate that reduces boilerplate code by providing repository interfaces and automatically generating query implementations.
